
/*let sidebarSwitch = false;

function toggleSidebar() {

  if (sidebarSwitch == true) {
    $("body").css("  grid-template-columns", "0 1fr");
    $( ".sidebar" ).css( "display", "none" );
    $(".header").css("grid-column", "1 / 3");
    sidebarSwitch = false;
  }

  else {
    $("body").css("  grid-template-columns", "clamp(200px, 15%, 500px) 3fr");
    $( ".sidebar" ).css( "display", "" );
    $(".header").css("grid-column", "1 / 3");
    sidebarSwitch = true;

  }
}; */